# Ops Observer

Responsibilities:

- Query logs, metrics, traces, and deploy history through controlled tools.
- Correlate incidents with recent changes.
- Produce evidence and hypotheses, not unapproved production actions.
