# Specs

Specs are the project memory and delivery contract.

Recommended states:

- `draft`: captured but not ready for implementation.
- `ready`: approved for implementation.
- `done`: implemented and verified.

Create a new spec:

```bash
npm run spec:new -- "short change title"
```

Move specs manually between folders until a real workflow engine is added.
